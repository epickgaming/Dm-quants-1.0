# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.10
pyarrow>=12.0

# Matrix-profile pattern discovery (default discovery engine)
stumpy>=1.12

# Big-data vectorized backtester (optional, used for portfolio stats / sweeps)
vectorbt>=0.25

# Free historical data source (optional; pipeline runs offline on synthetic data)
dukascopy-python>=4.0

# Config / CLI
pyyaml>=6.0

# Tests
pytest>=7.0
